# <Bug title — concise, symptom-first, e.g. "Export button disabled after second click">

**Severity:** <Critical / High / Medium / Low — see references/severity-guide.md>

## Repro steps

1. <step>
2. <step>
3. <step>

**Expected:** <what should happen>
**Actual:** <what actually happens>

**Environment:** <browser/OS/app version/account type — whatever's relevant
and known>

## Impacted areas

- `<file/module/service>` or <plain-text area name if no ticket exists yet>

## Dependencies

*(Only include this section if a real sequencing relationship exists to an
existing ticket — do not invent one.)*

Blocked by #<N>

*(If this section is present, the `dependency` label must also be applied
to this issue once created — both together, per the spec.)*

## Points

<n> — <one-line rationale> *(optional at intake — add later if triage
hasn't produced an estimate yet)*

---

Not yet created on GitHub. Confirm before I file this with `gh issue
create --label bug`.
